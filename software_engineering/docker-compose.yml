services:
  api:
    image: reco:1.0.0
    container_name: "reco"
    build:
      dockerfile: docker/dockerfile
      args:
        - APP_PORT=${APP_PORT}
    ports:
      - "${APP_PORT}:${APP_PORT}"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
